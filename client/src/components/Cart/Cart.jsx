import React from 'react';
import { Link } from 'react-router-dom';

function Cart() {

    //axios get to populate mapped cart component
    //map information to show what is in cart.json

    return(
        <div className="shop">
            <h2 className="shop__title">Cart</h2>
        </div>
    );
}

export default Cart;